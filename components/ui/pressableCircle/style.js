import { StyleSheet } from "react-native";

export default StyleSheet.create({
    buttonContainer: {
        // marginBottom: 40,
        height: 50,
        width: 50,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 50,
    },
    buttonStart: {
        fontFamily: "Toons",
        color: "white",
        fontSize: 30,
    },
});
