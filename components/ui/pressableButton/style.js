import { StyleSheet } from "react-native";

export default StyleSheet.create({
    buttonContainer: {
        marginBottom: 20,
        height: 50,
        width: 160,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 10,
    },
    buttonStart: {
        fontFamily: "Toons",
        color: "white",
        fontSize: 30,
    },
});
